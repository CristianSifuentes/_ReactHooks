# Advanced React Hooks Demo

This project demonstrates the practical differences between:

- `useEffect` (synchronizer for side effects)
- `useMemo` (value cache for expensive calculations)
- `useCallback` (function cache for stable references)

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
